export async function generateScript({ prompt, language, tone }) {
  // Replace with a real LLM call in production.
  return `Hook: Stop scrolling.\n\nMain: ${prompt}\n\nStyle: ${tone}. Language: ${language}.\n\nCTA: Follow for more useful AI videos.`;
}
